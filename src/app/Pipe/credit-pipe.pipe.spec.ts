import { CreditPipePipe } from './credit-pipe.pipe';

describe('CreditPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditPipePipe();
    expect(pipe).toBeTruthy();
  });
});
